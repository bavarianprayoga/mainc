#include <stdio.h>
#include <math.h>

int main(){

    double a = sqrt(9); //akar
    double b = pow(2, 4); //pangkat, 2 angkanya 4 pangkatnya
    int c = round(3.14); //pembulatan
    int d = ceil(3.14); //pembulatan ke atas
    int e = floor(3.14); //pembulatan ke bawah
    double f = fabs(-99); //nilai mutlak
    double g = log(8); //logaritma
    double h = sin(45); //sin
    double i = cos(45); //cos
    double j = tan(45); //tan


    return 0;
}